apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "dsv4.serviceAccountName" . }}
  labels:
    {{- include "dsv4.labels" . | nindent 4 }}
# The engines talk HTTP and ZMQ only; they never call the Kubernetes API.
automountServiceAccountToken: false
