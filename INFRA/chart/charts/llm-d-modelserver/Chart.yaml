apiVersion: v2
name: llm-d-modelserver
description: >-
  vLLM/SGLang model servers for the llm-d precise prefix-cache routing well-lit
  path, plus the standalone `vllm launch render` tokenizer Service the EPP
  token-producer plugin calls. Helm port of the llm-d v0.9.0 kustomize overlays.
type: application
version: 0.9.0
appVersion: v0.26.0
