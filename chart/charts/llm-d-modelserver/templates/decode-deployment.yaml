{{- if .Values.decode.enabled }}
{{- /*
  Primary model server. Aggregated (prefill OFF): role=prefill-decode, named
  <release>-modelserver. Disaggregated (prefill ON): role=decode, <release>-decode.
*/ -}}
{{- include "llm-d-modelserver.serverDeployment" (dict "root" $ "role" (include "llm-d-modelserver.decodeRole" .) "nameSuffix" (include "llm-d-modelserver.decodeSuffix" .) "cfg" .Values.decode) }}
{{- end }}
